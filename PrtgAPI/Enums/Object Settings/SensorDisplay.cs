namespace Prtg
{
    /// <summary>
    /// Specifies sensor display settings, found under the "Sensor Display" settings of object settings pages.
    /// </summary>
    public enum SensorDisplay
    {
        //Sensor Display

        //PrimaryChannel, //gets the id of the channel - it's an integer

        //Stack,

        //StackUnit - doesn't seem to work
    }
}
